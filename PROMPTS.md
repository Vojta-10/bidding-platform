# Gemini CLI - Prompt Repository

This file serves as a scratchpad for prompts generated by Gemini CLI. Copy and paste these into other agents (like Claude Code or ChatGPT) to continue the implementation workflow.

---

## 2026-04-28: Dashboard Refactor (Two-Column Grid & Component Reuse)

**Target:** Claude Code / Generalist Agent

```markdown
I need to refactor the Dashboard to implement the "Two-column grid" section as per our design plan.

### Goals:
1.  **Refactor Shared Components:** Move `BidCard.tsx` and `BidsCarousel.tsx` from `components/layout/dashboard/ActiveBids/` to `components/layout/dashboard/` to make them shared components. Update all imports.
2.  **Make `BidCard` Reusable:**
    *   Update `BidCardData` interface: Make `status` optional.
    *   Update `BidCard` component: 
        *   If `status` is provided (Active Bids), show the "Winning/Outbid" badge and "Raise Bid" button (if outbid).
        *   If `status` is NOT provided (Watching), hide the status badge and show a "Place Bid" button (using `Gavel` icon from lucide-react).
3.  **Create `RecentActivity` Component:**
    *   Create `components/layout/dashboard/RecentActivity.tsx`.
    *   It should be a `Card` containing a vertical scrolling list of activity items.
    *   Each item shows an icon (e.g., `Gavel`, `Trophy`, `User`), a description (e.g., "You were outbid on X"), and a relative timestamp (e.g., "2m ago").
4.  **Update `app/dashboard/page.tsx`:**
    *   Implement a two-column grid below the `ActiveBids` section.
    *   **Left Column (lg:col-span-2):** "Watching" section. Use the same `Card` wrapper as `ActiveBids`, but use `BidsCarousel` with mock data for items the user is watching.
    *   **Right Column (lg:col-span-1):** The `RecentActivity` component.
5.  **Data:**
    *   Add `MOCK_WATCHING_BIDS` (without status) and `MOCK_ACTIVITY` to provide a realistic preview.

### Technical Constraints:
- Use Tailwind CSS 4 for styling.
- Ensure the layout is responsive (stacks on mobile, 2-cols on `lg` screens).
- Maintain the "Amber" theme and standard dashboard spacing.
- Follow Next.js 16 conventions.
```
